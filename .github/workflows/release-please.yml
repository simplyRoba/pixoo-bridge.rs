name: Release Please

on:
  push:
    branches: [main]

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - name: Run release-please
        uses: googleapis/release-please-action@v4
        with:
          release-type: rust
          package-name: pixoo-bridge.rs
          changelog-path: CHANGELOG.md
          include-v-in-tag: true
          version-file: Cargo.toml
        id: release
